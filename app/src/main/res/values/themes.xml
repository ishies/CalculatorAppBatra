<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalculatorAppBatra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CalculatorAppBatra" parent="Base.Theme.CalculatorAppBatra" />

    <style name="button" parent ="Widget.AppCompat.Button">
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:backgroundTint">@color/TextAndButtons</item>
        <item name="cornerRadius">0dp</item>
        <item name="android:textColor">@color/background</item>
        <item name="android:textSize">@dimen/heading</item>
    </style>

    <style name="buttonSmall" parent="button">
        <item name="android:textSize">@dimen/heading2</item>
        <item name="android:layout_margin">@dimen/marginSmall</item>
        <item name="android:layout_width">86dp</item>
        <item name="android:layout_height">86dp</item>
    </style>
</resources>